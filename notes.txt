TODO:
- simplify ui code (more automated row computation)
- construct model list inside primitive creator?
- is renderCountStringsCount even needed?
- fix m_vertexCount vs GetVertexCount, also indexCount
- clean up superfluous virtual specifiers in ineriting models
- PrimitiveType: enum -> enum class 
- pass frustum or create locally?
- figure out where and how to track render counts.
 - dataflow back from primitive creator to graphics?
- isInsideFrustum() for other classes
- "templates" for similar ui-code for new classes
- where to put desiredModelCount(s)?
- clarify ModelClass -> m_SphereModel 
- store ui message positions in one place (duplicated in init, update)
- simplify ALL shutdown and initialize functions in the program
- break up ConstructAndInitialize template
 - split the window message off 
 - lambda to combine repetitve code blocks
  - communicates: local refinement for readability
- bug?: render count strings updated before frame is rendered
 -> counts outdated by one frame?
- simplify ui message function interfaces: 
 - initialize: ScreenDims, FloatColor
- compress primitive creator's render code
- write a shader to transform vertices in array 
 -> use it two compute a true aabb
 -> use it for proper culling of transformed (rotated) objects 
- is there a better/safer way to iterate over enum value count?
 -> init of model list class
- move includes from header to impl where applicable

-individual ctors/init:
 - e.g. pin model: ctor that only sets circleSides?
 - static vertex counts?

- nice random colors
- nice color gradients
- 
- understand this:
 // Reset to the original world matrix.
 Direct3D->GetWorldMatrix(worldMatrix);

-keep local copy of D3DClass?
 -or use doled-out device and deviceContext?
-shared_ptr<D3DClass> Direct3D for Graphics, PrimitiveCreator

- is it feasible to have sphere model inherit base model?
 - initialize function has a different signature
  -> breaks liskov s.p.?
- refactor all classes into aggregates? (as long as it makes sense)
- hwnd error message for failed aabb initialization?
 -> pass hwnd to model class
- what if we make UI rowHeight was const?

- declarative primitive/geometry definition?
- primitives are just strategies for the model class?
 -> prototypes?
 
 - do something about unused color params in model info list
 - why do i have to manually update initialize,shutdown,render for every new primitive?
  -> there's got to be a way to have the code automatically update itself
NOTES:
-XMStoreFloat4x4, XMLoadFloat4x4 used in Frustum

-forced control flow: Graphics::Frame() is public, Graphics::Render() is private. Render() takes a D3DClass* but Graphics doesn't have a D3DClass member. Hence it follows that Render will be called be a public method, expecting a user of the class to provide the D3D instance.

EXAM PRESET:
- uses IsSphereInsideFrustum() for all types

- #include <d3dx10math.h> 
 - in LightClass.h
 - not found 
 - doesn't exist in current windows sdk
 -> vs: exclude from project
 